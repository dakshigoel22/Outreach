---
phase: 2
plan: "02-03"
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - database/companies.py
  - utils/session.py
autonomous: true
requirements:
  - DISC-02

must_haves:
  truths:
    - "save_company(company) inserts all 8 fields with parameterized queries; hiring_roles serialized as comma-joined TEXT"
    - "get_saved_company_identifiers() returns two sets: lowercase names and normalized (lowercase, trailing-slash-stripped) URLs"
    - "Both functions use get_connection() — no new sqlite3.connect() calls"
    - "SESSION_DEFAULTS in utils/session.py contains discover_location, discover_tier, discover_role_type"
  artifacts:
    - path: "database/companies.py"
      provides: "save_company(company: Company) and get_saved_company_identifiers() -> tuple[set[str], set[str]]"
      exports: ["save_company", "get_saved_company_identifiers"]
    - path: "utils/session.py"
      provides: "SESSION_DEFAULTS updated with 3 new filter keys"
      contains: "discover_location"
  key_links:
    - from: "database/companies.py"
      to: "database/connection.py"
      via: "get_connection() for all DB access"
      pattern: "get_connection"
    - from: "database/companies.py"
      to: "services/claude.py"
      via: "Company type import for save_company signature"
      pattern: "from services.claude import Company"
    - from: "utils/session.py SESSION_DEFAULTS"
      to: "pages/1_Discover.py st.selectbox key="
      via: "filter keys wired as selectbox key= parameter"
      pattern: "discover_location"
---

<objective>
Create database/companies.py with save_company() and get_saved_company_identifiers(), and update utils/session.py SESSION_DEFAULTS with the three filter persistence keys required by D-06.

Purpose: Provides the persistence and duplicate-detection layer that the Discover page (plan 02-04) calls directly. Also completes the session state contract that the filter selectboxes depend on.

Output: database/companies.py with two functions; utils/session.py with 3 new SESSION_DEFAULTS keys.
</objective>

<execution_context>
@/Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-RESEARCH.md §Architecture Patterns — Pattern 7 (batch duplicate check), Pattern 8 (INSERT)
@/Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-RESEARCH.md §Code Examples — Duplicate Check and Save in database/companies.py
@/Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-CONTEXT.md §D-06 (filter session state), §D-08 (duplicate detection), §D-09 (batch query)
</execution_context>

<context>
@/Users/dakshigoel/Outreach/.planning/PROJECT.md
@/Users/dakshigoel/Outreach/.planning/ROADMAP.md

<interfaces>
<!-- Key types and contracts. Extracted from existing codebase and plan 02-02. -->

From database/connection.py:
```python
@st.cache_resource
def get_connection() -> sqlite3.Connection:
    """Cached connection. Always use this — do NOT call sqlite3.connect() directly."""
    # conn.row_factory = sqlite3.Row — row["column_name"] access
```

From database/schema.py (companies table — Phase 1, no changes in Phase 2):
```
companies(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  funding_stage TEXT,
  headcount TEXT,
  website TEXT,
  ai_focus TEXT,
  location TEXT,
  tier TEXT,
  hiring_roles TEXT,  ← comma-joined string, NOT a list
  saved_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

From services/claude.py (plan 02-02):
```python
class Company(BaseModel):
    name: str
    funding_stage: Optional[str] = None
    headcount: Optional[str] = None
    website: Optional[str] = None
    ai_focus: Optional[str] = None
    location: Optional[str] = None
    tier: Optional[str] = None
    hiring_roles: list[str] = []  # serialize to comma-joined TEXT on INSERT
```

From utils/session.py (current SESSION_DEFAULTS — existing keys):
```python
SESSION_DEFAULTS: dict = {
    "profile_loaded": False,
    "profile": {},
    "selected_company_id": None,
    "selected_contact_id": None,
    "discover_results": [],
    "discover_running": False,
    "draft_email_text": "",
    "draft_contact_id": None,
    "tracker_filter_status": "All",
    "dashboard_filter": {},
    # MISSING — must add:
    # "discover_location": "NYC",
    # "discover_tier": "Both",
    # "discover_role_type": "Both",
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create database/companies.py with save and duplicate-check functions</name>
  <files>database/companies.py</files>
  <read_first>
    /Users/dakshigoel/Outreach/database/connection.py — get_connection() signature and row_factory pattern.
    /Users/dakshigoel/Outreach/database/schema.py — companies table column names (exact spelling required for INSERT).
    /Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-RESEARCH.md §Pattern 7 and Pattern 8.
  </read_first>
  <behavior>
    - get_saved_company_identifiers() returns (set(), set()) when companies table is empty
    - get_saved_company_identifiers() returns ({"acme"}, {"https://acme.com"}) for a row with name="Acme", website="https://acme.com/"
    - get_saved_company_identifiers() handles rows where website is None (excluded from url set)
    - get_saved_company_identifiers() handles rows where name is None (excluded from name set)
    - save_company(company) inserts a row; subsequent get_saved_company_identifiers() returns name in set
    - save_company() serializes hiring_roles=["ML","NLP"] as "ML,NLP" TEXT in DB
    - save_company() serializes hiring_roles=[] as None in DB (not "")
    - All SQL uses ? parameterized placeholders — no f-strings with user data
  </behavior>
  <action>
    Create database/companies.py.

    Module docstring: "Company persistence and duplicate detection for Outreach CRM."

    Import: from database.connection import get_connection; from services.claude import Company.

    save_company(company: Company) -> None:
    - Docstring: "Insert a company. Caller is responsible for duplicate check before calling (badge hides Save button for already-saved companies)."
    - Calls get_connection()
    - Executes INSERT INTO companies (name, funding_stage, headcount, website, ai_focus, location, tier, hiring_roles) VALUES (?, ?, ?, ?, ?, ?, ?, ?) with params tuple: (company.name, company.funding_stage, company.headcount, company.website, company.ai_focus, company.location, company.tier, ",".join(company.hiring_roles) if company.hiring_roles else None)
    - Calls conn.commit()
    - No f-string SQL — parameterized only.

    get_saved_company_identifiers() -> tuple[set[str], set[str]]:
    - Docstring: "Return (lowercase_names, normalized_urls) sets for O(1) duplicate detection (per D-08, D-09)."
    - Calls get_connection()
    - Executes: cursor = conn.execute("SELECT name, website FROM companies")
    - rows = cursor.fetchall()
    - names = {row["name"].lower() for row in rows if row["name"]}
    - urls = {row["website"].rstrip("/").lower() for row in rows if row["website"]}
    - Returns (names, urls)

    __all__ = ["save_company", "get_saved_company_identifiers"]

    CRITICAL: Do NOT use f-string SQL (e.g., f"INSERT INTO ... WHERE name='{x}'"). All values via ? placeholders.
    CRITICAL: Do NOT call sqlite3.connect() directly. Always get_connection().
    CRITICAL: hiring_roles must be serialized with ",".join() — SQLite has no list type.
  </action>
  <verify>
    <automated>cd /Users/dakshigoel/Outreach && python3 -c "
import sqlite3, tempfile, os, pathlib

# Use an isolated in-memory DB for testing — patch get_connection
import database.connection as conn_module
import streamlit as st

# Create an in-memory connection for testing
_test_conn = sqlite3.connect(':memory:', check_same_thread=False)
_test_conn.row_factory = sqlite3.Row
_test_conn.execute('''CREATE TABLE companies (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    funding_stage TEXT, headcount TEXT, website TEXT,
    ai_focus TEXT, location TEXT, tier TEXT, hiring_roles TEXT,
    saved_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)''')
_test_conn.commit()

# Monkeypatch
original = conn_module.get_connection
conn_module.get_connection = lambda: _test_conn

from database.companies import save_company, get_saved_company_identifiers
from services.claude import Company

# Empty state
names, urls = get_saved_company_identifiers()
assert names == set(), f'Expected empty set, got {names}'
assert urls == set(), f'Expected empty url set, got {urls}'

# Save a company
c = Company(name='Acme AI', website='https://acme.com/', hiring_roles=['ML', 'NLP'], funding_stage='Series A')
save_company(c)

names, urls = get_saved_company_identifiers()
assert 'acme ai' in names, f'name not found: {names}'
assert 'https://acme.com' in urls, f'url not found: {urls}'

# Verify hiring_roles serialized correctly
row = _test_conn.execute('SELECT hiring_roles FROM companies WHERE name=?', ('Acme AI',)).fetchone()
assert row['hiring_roles'] == 'ML,NLP', f'Got: {row[\"hiring_roles\"]}'

# Restore
conn_module.get_connection = original
print('database/companies.py OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - database/companies.py exports save_company and get_saved_company_identifiers
    - save_company's INSERT statement uses 8 ? placeholders — no f-string SQL
    - hiring_roles serialized as comma-joined string (",".join) — not a JSON array, not a list
    - get_saved_company_identifiers returns tuple of two sets (lowercase names, lowercased+stripped URLs)
    - get_connection() imported from database.connection (no direct sqlite3.connect())
    - python3 -c "from database.companies import save_company, get_saved_company_identifiers" exits 0
  </acceptance_criteria>
  <done>database/companies.py implemented; save_company() and get_saved_company_identifiers() verified correct with parameterized SQL and comma-joined hiring_roles.</done>
</task>

<task type="auto">
  <name>Task 2: Add filter persistence keys to SESSION_DEFAULTS in utils/session.py</name>
  <files>utils/session.py</files>
  <read_first>
    /Users/dakshigoel/Outreach/utils/session.py — full current content; must see all existing keys before modifying.
    /Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-UI-SPEC.md §Session State Keys Added in Phase 2 — exact keys, defaults, and types required.
    /Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-CONTEXT.md §D-06 — filter selections persist in session state.
  </read_first>
  <action>
    Edit utils/session.py SESSION_DEFAULTS dict to add exactly these three keys (insert after "discover_running": False):
      "discover_location": "NYC",
      "discover_tier": "Both",
      "discover_role_type": "Both",

    The existing 10 keys must be preserved unchanged. Only add the 3 new keys.

    Do NOT change init_session_state() — it already iterates SESSION_DEFAULTS and sets missing keys; the new keys are automatically handled.

    Final SESSION_DEFAULTS must have exactly 13 keys total:
      profile_loaded, profile, selected_company_id, selected_contact_id,
      discover_results, discover_running,
      discover_location, discover_tier, discover_role_type,  ← NEW
      draft_email_text, draft_contact_id, tracker_filter_status, dashboard_filter

    Default values:
      discover_location = "NYC"    (matches first selectbox option per UI-SPEC)
      discover_tier = "Both"       (broadest default per UI-SPEC)
      discover_role_type = "Both"  (broadest default per UI-SPEC)
  </action>
  <verify>
    <automated>cd /Users/dakshigoel/Outreach && python3 -c "
from utils.session import SESSION_DEFAULTS
assert 'discover_location' in SESSION_DEFAULTS, 'discover_location missing'
assert 'discover_tier' in SESSION_DEFAULTS, 'discover_tier missing'
assert 'discover_role_type' in SESSION_DEFAULTS, 'discover_role_type missing'
assert SESSION_DEFAULTS['discover_location'] == 'NYC', f'wrong default: {SESSION_DEFAULTS[\"discover_location\"]}'
assert SESSION_DEFAULTS['discover_tier'] == 'Both', f'wrong default: {SESSION_DEFAULTS[\"discover_tier\"]}'
assert SESSION_DEFAULTS['discover_role_type'] == 'Both', f'wrong default: {SESSION_DEFAULTS[\"discover_role_type\"]}'
assert len(SESSION_DEFAULTS) == 13, f'Expected 13 keys, got {len(SESSION_DEFAULTS)}: {list(SESSION_DEFAULTS.keys())}'
# Existing keys preserved
for k in ['profile_loaded','profile','selected_company_id','selected_contact_id',
          'discover_results','discover_running','draft_email_text','draft_contact_id',
          'tracker_filter_status','dashboard_filter']:
    assert k in SESSION_DEFAULTS, f'Existing key missing: {k}'
print('SESSION_DEFAULTS OK — 13 keys')
"</automated>
  </verify>
  <acceptance_criteria>
    - SESSION_DEFAULTS contains "discover_location" with default "NYC"
    - SESSION_DEFAULTS contains "discover_tier" with default "Both"
    - SESSION_DEFAULTS contains "discover_role_type" with default "Both"
    - SESSION_DEFAULTS has exactly 13 keys (10 original + 3 new)
    - All 10 original keys are preserved with their original defaults
    - init_session_state() function is unchanged (no modification needed)
  </acceptance_criteria>
  <done>SESSION_DEFAULTS updated with 3 filter keys; all existing keys preserved; init_session_state() covers new keys automatically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| services/claude.py → database/companies.py | Company object (Claude-generated data) enters the DB layer; all fields must reach DB via parameterized queries |
| database/connection.py → SQLite file | get_connection() provides the single trusted connection; no bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | save_company() INSERT with Claude-generated company.name | mitigate | All 8 fields inserted via ? parameterized placeholders — SQL injection impossible regardless of content |
| T-02-08 | Tampering | get_saved_company_identifiers() SELECT — no WHERE clause | accept | Full table scan with no user input in query; SELECT with no parameters has no injection surface |
| T-02-09 | Tampering | hiring_roles comma-join serialization | accept | Data goes through pydantic Company.model_validate() before save_company(); list type enforced by pydantic; comma-join is safe string operation |
| T-02-SC | Tampering | No new package installs in this plan | accept | database/companies.py uses only stdlib sqlite3 and existing project imports; no new packages |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/dakshigoel/Outreach
python3 -c "from database.companies import save_company, get_saved_company_identifiers; print('DB layer imports OK')"
python3 -c "
from utils.session import SESSION_DEFAULTS
assert len(SESSION_DEFAULTS) == 13
assert SESSION_DEFAULTS['discover_location'] == 'NYC'
assert SESSION_DEFAULTS['discover_tier'] == 'Both'
assert SESSION_DEFAULTS['discover_role_type'] == 'Both'
print('SESSION_DEFAULTS OK')
"
grep -v "^#" database/companies.py | grep -c "f\""
# ^ should return 0 (no f-string SQL)
```
</verification>

<success_criteria>
- database/companies.py exports save_company and get_saved_company_identifiers
- All SQL in database/companies.py uses ? parameterized queries — zero f-string SQL
- hiring_roles serialized to comma-joined TEXT on INSERT
- get_saved_company_identifiers returns two Python sets (lowercased names, normalized URLs)
- SESSION_DEFAULTS has 13 keys including discover_location, discover_tier, discover_role_type
- New session defaults: discover_location="NYC", discover_tier="Both", discover_role_type="Both"
</success_criteria>

<output>
Create `.planning/phases/02-discovery/02-03-SUMMARY.md` when done.
</output>
