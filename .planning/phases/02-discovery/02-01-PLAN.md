---
phase: 2
plan: "02-01"
type: execute
wave: 0
depends_on: []
files_modified:
  - requirements.txt
  - .env.example
autonomous: true
requirements:
  - DISC-01

must_haves:
  truths:
    - "anthropic==0.104.1 is installed and importable (web_search requires >= 0.51.0)"
    - "ANTHROPIC_API_KEY is documented in .env.example"
    - "pytest is installable and the tests/ directory skeleton exists"
  artifacts:
    - path: "requirements.txt"
      provides: "anthropic pinned to 0.104.1; pytest added"
      contains: "anthropic==0.104.1"
    - path: ".env.example"
      provides: "ANTHROPIC_API_KEY template key for developer setup"
      contains: "ANTHROPIC_API_KEY"
    - path: "tests/__init__.py"
      provides: "test package marker"
  key_links:
    - from: "requirements.txt"
      to: "pip environment"
      via: "pip install -r requirements.txt"
      pattern: "anthropic==0.104.1"
---

<objective>
Upgrade the Anthropic SDK from 0.28.0 to 0.104.1 (web_search server tool requires >= 0.51.0), add pytest to requirements.txt, verify the .env contains ANTHROPIC_API_KEY, and create the tests/ package skeleton.

Purpose: Wave 0 gate — no Claude code can run on 0.28.0. This plan unblocks every subsequent plan in Phase 2. Must complete before any services/ or pages/ code is written.

Output: requirements.txt updated, pip environment upgraded, tests/ skeleton present, .env.example updated.
</objective>

<execution_context>
@/Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-RESEARCH.md §Environment Availability
@/Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-RESEARCH.md §Common Pitfalls — Pitfall 1 (SDK Version Mismatch)
</execution_context>

<context>
@/Users/dakshigoel/Outreach/.planning/PROJECT.md
@/Users/dakshigoel/Outreach/.planning/ROADMAP.md

<!-- Wave 0: no prior SUMMARYs. This plan upgrades the environment before any service code exists. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade anthropic SDK and add pytest to requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>
    /Users/dakshigoel/Outreach/requirements.txt — current contents; line containing anthropic==0.28.0 must be updated to anthropic==0.104.1.
    /Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-RESEARCH.md §Standard Stack — confirms 0.104.1 as latest verified version.
  </read_first>
  <action>
    Edit requirements.txt: change the line "anthropic==0.28.0" to "anthropic==0.104.1".

    Add a new line "pytest==8.2.2" to requirements.txt (after the existing entries).

    Then run: pip install -r requirements.txt

    After pip install, verify the upgrade took effect by importing anthropic and checking its version string contains "0.104.1".

    Do NOT change any other pinned versions (streamlit==1.35.0, pandas==2.1.4, etc. stay unchanged).
  </action>
  <verify>
    <automated>cd /Users/dakshigoel/Outreach && python3 -c "import anthropic; v=anthropic.__version__; assert v=='0.104.1', f'Got {v}'; print('anthropic', v, 'OK')" && python3 -m pytest --version</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt line for anthropic reads exactly "anthropic==0.104.1" (not 0.28.0)
    - requirements.txt contains a line "pytest==8.2.2"
    - python3 -c "import anthropic" exits 0 with no ImportError or TypeError
    - python3 -m pytest --version exits 0
    - No other version pins in requirements.txt are changed
  </acceptance_criteria>
  <done>anthropic 0.104.1 installed and importable; pytest available; requirements.txt updated.</done>
</task>

<task type="auto">
  <name>Task 2: Verify .env has ANTHROPIC_API_KEY and create tests/ skeleton</name>
  <files>
    .env.example,
    tests/__init__.py
  </files>
  <read_first>
    /Users/dakshigoel/Outreach/.env.example — check whether ANTHROPIC_API_KEY key is already present.
    /Users/dakshigoel/Outreach/.planning/phases/02-discovery/02-RESEARCH.md §Environment Availability — ANTHROPIC_API_KEY listed as "Not verified".
  </read_first>
  <action>
    Update .env.example to ensure it contains these two keys (preserve any existing content; add missing keys only):
      ANTHROPIC_API_KEY=your-key-here
      DB_PATH=outreach.db

    Check whether a .env file exists at the project root. If it does NOT exist, print a warning message but do NOT create one — that is the user's responsibility (the key is a secret). If it does exist, verify it contains "ANTHROPIC_API_KEY=" (any value). Do NOT read or log the key value.

    Create tests/__init__.py as an empty file (package marker). This makes "tests" a Python package so pytest can discover test modules inside it.

    Do NOT create test_claude_service.py or test_companies_db.py here — those are Wave 4 (plan 02-05).
  </action>
  <verify>
    <automated>cd /Users/dakshigoel/Outreach && grep -c "ANTHROPIC_API_KEY" .env.example && python3 -c "import pathlib; assert pathlib.Path('tests/__init__.py').exists(), 'tests/__init__.py missing'; print('tests/ skeleton OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains "ANTHROPIC_API_KEY=your-key-here" (or any placeholder value)
    - .env.example contains "DB_PATH=outreach.db"
    - tests/__init__.py exists (file may be empty)
    - No .env file created by this task (user must create it manually with their real key)
    - No API key value appears in any committed file
  </acceptance_criteria>
  <done>.env.example documents required keys; tests/ package skeleton created; no secrets committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → app | ANTHROPIC_API_KEY flows from .env into Anthropic() client; must not appear in requirements.txt or any committed file |
| pip install | Third-party packages pulled from PyPI; anthropic 0.104.1 is the official Anthropic SDK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | ANTHROPIC_API_KEY in .env | mitigate | .env in .gitignore (verified by Phase 1); only .env.example committed; task verifies key existence without logging value |
| T-02-02 | Tampering | pip install anthropic==0.104.1 | mitigate | anthropic is [OK] per Package Legitimacy Audit in RESEARCH.md; official SDK at github.com/anthropics/anthropic-sdk-python |
| T-02-SC | Tampering | requirements.txt package installs | mitigate | All packages are [OK] per RESEARCH.md Package Legitimacy Audit; slopcheck verified 2026-05-25 |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/dakshigoel/Outreach
python3 -c "import anthropic; print('anthropic', anthropic.__version__)"
python3 -m pytest --version
grep "anthropic==0.104.1" requirements.txt
grep "pytest==8.2.2" requirements.txt
grep "ANTHROPIC_API_KEY" .env.example
python3 -c "import pathlib; assert pathlib.Path('tests/__init__.py').exists(); print('tests/ skeleton OK')"
```

All commands must exit 0 with expected output.
</verification>

<success_criteria>
- anthropic.__version__ == "0.104.1"
- pytest --version exits 0
- requirements.txt contains anthropic==0.104.1 and pytest==8.2.2
- .env.example contains ANTHROPIC_API_KEY key
- tests/__init__.py exists
- No ANTHROPIC_API_KEY value appears in any committed file
</success_criteria>

<output>
Create `.planning/phases/02-discovery/02-01-SUMMARY.md` when done.
</output>
