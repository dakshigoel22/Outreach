---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pages/1_Discover.py
  - pages/2_Contacts.py
  - pages/3_Draft_Email.py
  - pages/4_Tracker.py
  - pages/5_Dashboard.py
  - utils/badges.py
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "Sidebar nav shows all six page names: Discover, Contacts, Draft Email, Tracker, Dashboard, My Profile"
    - "Navigating to any shell page shows the page title and a coming-soon info banner — no error"
    - "Navigating directly to any page via URL produces no KeyError in session state"
    - "utils/badges.py defines STATUS_COLORS and status_badge() for future use in Phase 4"
  artifacts:
    - path: "pages/1_Discover.py"
      provides: "Shell page with title and st.info('Coming soon — built in Phase 2.')"
    - path: "pages/2_Contacts.py"
      provides: "Shell page with title and st.info('Coming soon — built in Phase 3.')"
    - path: "pages/3_Draft_Email.py"
      provides: "Shell page with title and st.info('Coming soon — built in Phase 3.')"
    - path: "pages/4_Tracker.py"
      provides: "Shell page with title and st.info('Coming soon — built in Phase 4.')"
    - path: "pages/5_Dashboard.py"
      provides: "Shell page with title and st.info('Coming soon — built in Phase 4.')"
    - path: "utils/badges.py"
      provides: "STATUS_COLORS dict and status_badge(status: str) -> str"
      exports: ["STATUS_COLORS", "status_badge"]
  key_links:
    - from: "pages/1_Discover.py"
      to: "utils/session.py"
      via: "init_session_state() prevents KeyError on cold navigation"
      pattern: "init_session_state"
---

<objective>
Create the five shell pages (Discover, Contacts, Draft Email, Tracker, Dashboard) and the status badge utility. This completes the full multi-page nav structure and fulfills the FOUND-02 requirement.

Purpose: The app must show all six pages in the sidebar on first run. Shells prove nav is wired, session state is safe on cold load, and the badge utility is ready before Phase 4 needs it.

Output: Five shell page files + utils/badges.py. No functional logic on shell pages — exactly st.title() + st.info() per UI-SPEC.
</objective>

<execution_context>
@/Users/dakshigoel/Outreach/.planning/phases/01-foundation/01-UI-SPEC.md
</execution_context>

<context>
@/Users/dakshigoel/Outreach/.planning/ROADMAP.md
@/Users/dakshigoel/Outreach/.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From utils/session.py (created in Plan 01-01) -->
From utils/session.py:
  def init_session_state() -> None
    # Pre-initializes all 10 session state keys; safe to call on every page

<!-- From UI-SPEC.md §Status Badge Colors -->
STATUS_COLORS mapping (6 statuses):
  Not Contacted: bg=#e5e7eb, text=#374151
  Drafted:       bg=#fef3c7, text=#92400e
  Sent:          bg=#dbeafe, text=#1e40af
  Replied:       bg=#d1fae5, text=#065f46
  Interview:     bg=#16a34a, text=#ffffff
  Rejected:      bg=#fee2e2, text=#991b1b
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Five shell pages</name>
  <files>
    pages/1_Discover.py,
    pages/2_Contacts.py,
    pages/3_Draft_Email.py,
    pages/4_Tracker.py,
    pages/5_Dashboard.py
  </files>
  <read_first>
    UI-SPEC.md §Navigation Structure — shell pages render ONLY st.title() + st.info(); no extra content.
    UI-SPEC.md §Copywriting Contract — shell pages: exact copy per page.
    UI-SPEC.md §Session State Initialization — every page calls init_session_state() as second call.
    UI-SPEC.md §Page Layout — every page calls st.set_page_config with identical args as first call.
  </read_first>
  <action>
    Create each shell page using the EXACT pattern below. Apply the page-specific title and info copy from the table. Do NOT add any widgets, headers, placeholders, or fake data beyond st.title() and st.info().

    Pattern for every shell page (substitute PAGE_TITLE and INFO_TEXT):

      import streamlit as st
      from utils.session import init_session_state

      st.set_page_config(
          page_title="Outreach CRM",
          page_icon=":outbox_tray:",
          layout="wide",
          initial_sidebar_state="expanded",
      )

      init_session_state()

      st.title("PAGE_TITLE")
      st.info("INFO_TEXT")

    Page-specific values:
      pages/1_Discover.py:
        PAGE_TITLE = "Discover"
        INFO_TEXT  = "Coming soon — built in Phase 2."

      pages/2_Contacts.py:
        PAGE_TITLE = "Contacts"
        INFO_TEXT  = "Coming soon — built in Phase 3."

      pages/3_Draft_Email.py:
        PAGE_TITLE = "Draft Email"
        INFO_TEXT  = "Coming soon — built in Phase 3."

      pages/4_Tracker.py:
        PAGE_TITLE = "Tracker"
        INFO_TEXT  = "Coming soon — built in Phase 4."

      pages/5_Dashboard.py:
        PAGE_TITLE = "Dashboard"
        INFO_TEXT  = "Coming soon — built in Phase 4."

    Do NOT use st.markdown, st.write, or st.subheader on shell pages.
    Do NOT import database or models modules on shell pages — they have no data needs.
  </action>
  <verify>
    <automated>python3 -c "
import ast, pathlib

pages = {
    'pages/1_Discover.py':    ('Discover',    'Coming soon — built in Phase 2.'),
    'pages/2_Contacts.py':    ('Contacts',    'Coming soon — built in Phase 3.'),
    'pages/3_Draft_Email.py': ('Draft Email', 'Coming soon — built in Phase 3.'),
    'pages/4_Tracker.py':     ('Tracker',     'Coming soon — built in Phase 4.'),
    'pages/5_Dashboard.py':   ('Dashboard',   'Coming soon — built in Phase 4.'),
}

for path, (title, info) in pages.items():
    src = pathlib.Path('/Users/dakshigoel/Outreach', path).read_text()
    assert 'set_page_config' in src, f'{path}: missing set_page_config'
    assert 'init_session_state' in src, f'{path}: missing init_session_state'
    assert title in src, f'{path}: missing title {title!r}'
    assert info in src, f'{path}: missing info text {info!r}'
    # No SQL or model imports on shell pages
    assert 'INSERT' not in src and 'SELECT' not in src, f'{path}: contains SQL'
    assert 'from models' not in src and 'from database' not in src, f'{path}: imports models/database'

print('All 5 shell pages OK')
"
</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist in pages/ with correct numeric prefixes (1_ through 5_)
    - Each page calls st.set_page_config as its first statement
    - Each page calls init_session_state() before any other logic
    - Each page calls exactly st.title("PAGE_TITLE") and st.info("INFO_TEXT") — nothing else
    - Info text matches UI-SPEC copywriting exactly (Phase 2, Phase 3, Phase 3, Phase 4, Phase 4)
    - No SQL, no model imports, no database imports on any shell page
  </acceptance_criteria>
  <done>Five shell pages exist; navigating to any of them shows title + coming-soon banner with no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Status badge utility</name>
  <files>
    utils/badges.py
  </files>
  <read_first>
    UI-SPEC.md §Status Badge Colors — exact STATUS_COLORS dict values and status_badge() function signature and implementation.
  </read_first>
  <action>
    Create utils/badges.py implementing STATUS_COLORS and status_badge() exactly as specified in UI-SPEC.md:

    STATUS_COLORS = {
        "Not Contacted": {"bg": "#e5e7eb", "text": "#374151"},
        "Drafted":       {"bg": "#fef3c7", "text": "#92400e"},
        "Sent":          {"bg": "#dbeafe", "text": "#1e40af"},
        "Replied":       {"bg": "#d1fae5", "text": "#065f46"},
        "Interview":     {"bg": "#16a34a", "text": "#ffffff"},
        "Rejected":      {"bg": "#fee2e2", "text": "#991b1b"},
    }

    def status_badge(status: str) -> str:
        c = STATUS_COLORS.get(status, {"bg": "#e5e7eb", "text": "#374151"})
        return (
            f'<span style="background-color:{c["bg"]};color:{c["text"]};'
            f'padding:2px 8px;border-radius:4px;font-size:13px;font-weight:600;">'
            f'{status}</span>'
        )

    Export: STATUS_COLORS, status_badge.

    This function is NOT called on any Phase 1 page. It is defined here for Phase 4 use.
    No streamlit import needed in this file — it is a pure Python utility.
  </action>
  <verify>
    <automated>python3 -c "
from utils.badges import STATUS_COLORS, status_badge

# Check all 6 statuses defined
expected = {'Not Contacted', 'Drafted', 'Sent', 'Replied', 'Interview', 'Rejected'}
assert set(STATUS_COLORS.keys()) == expected, f'Missing statuses: {expected - set(STATUS_COLORS.keys())}'

# Check specific colors
assert STATUS_COLORS['Interview']['bg'] == '#16a34a', 'Interview bg wrong'
assert STATUS_COLORS['Rejected']['bg'] == '#fee2e2', 'Rejected bg wrong'
assert STATUS_COLORS['Sent']['text'] == '#1e40af', 'Sent text wrong'

# Check badge output contains span and status text
badge = status_badge('Sent')
assert '<span' in badge and 'Sent' in badge and 'background-color:#dbeafe' in badge, f'Badge output wrong: {badge}'

# Fallback for unknown status
unknown = status_badge('Unknown')
assert '#e5e7eb' in unknown, 'Fallback color not applied'

print('badges.py OK')
"
</automated>
  </verify>
  <acceptance_criteria>
    - utils/badges.py exports STATUS_COLORS dict and status_badge function
    - STATUS_COLORS has exactly 6 keys: Not Contacted, Drafted, Sent, Replied, Interview, Rejected
    - STATUS_COLORS["Interview"]["bg"] == "#16a34a" (dark green)
    - STATUS_COLORS["Rejected"]["bg"] == "#fee2e2" (red)
    - status_badge("Sent") returns HTML containing background-color:#dbeafe and color:#1e40af
    - status_badge("Unknown") falls back to bg=#e5e7eb, text=#374151
    - No streamlit import in utils/badges.py
  </acceptance_criteria>
  <done>STATUS_COLORS and status_badge() defined; Phase 4 tracker can import and use without changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| status_badge() output → Streamlit unsafe_allow_html | HTML generated from STATUS_COLORS dict values; values are static constants not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | XSS | utils/badges.py status_badge() | mitigate | status_badge() interpolates only STATUS_COLORS constants and the status string from a closed enum — callers must validate status is a known key before passing to status_badge(); Phase 4 plan must enforce this |
| T-03-02 | Denial of Service | Shell pages with no data access | accept | Shell pages call only init_session_state() — no DB, no API; zero attack surface |
| T-03-03 | Information Disclosure | Shell pages | accept | Pages show static copy only; no user data rendered |
| T-03-SC | Tampering | pip install (no new packages this plan) | accept | No new dependencies introduced in this plan |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/dakshigoel/Outreach
python3 -c "
import pathlib

# All 5 shell pages exist
for name in ['1_Discover.py','2_Contacts.py','3_Draft_Email.py','4_Tracker.py','5_Dashboard.py']:
    assert pathlib.Path('pages', name).exists(), f'Missing: {name}'

# Badges utility works
from utils.badges import STATUS_COLORS, status_badge
assert len(STATUS_COLORS) == 6
badge = status_badge('Interview')
assert '#16a34a' in badge

print('Plan 03 verification passed')
"
```
</verification>

<success_criteria>
- All 5 shell page files exist in pages/ with correct numeric prefix filenames
- Each shell page calls set_page_config and init_session_state — verified by AST check
- utils/badges.py passes the full badge verification (6 statuses, correct hex values, fallback)
- No SQL or model imports appear in any shell page
- Streamlit multipage nav shows all 6 pages in sidebar when app runs
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done.
</output>
